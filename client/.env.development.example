# Speccify client (Vite) env template — DEVELOPMENT. Copy to `.env.development`.
# The frontend calls a same-origin, reverse-proxied path — never a backend host.
VITE_BASE_API_URL=/api
VITE_APP_ENV=development
