# Speccify client (Vite) env template — PROD (speccify.info). Copy to `.env.production`.
# Build with: `npm run build --workspace client` (default mode=production).
VITE_BASE_API_URL=/api
VITE_APP_ENV=production
